# Local development only. Copy to .env before the first database startup.
# On an existing volume, changing these values does not update database credentials.
POSTGRES_USER=cooper
POSTGRES_PASSWORD=cooperpass
POSTGRES_DB=cooper
# Metabase application storage uses public; project tables belong in analytics.
MB_DB_DBNAME=cooper
